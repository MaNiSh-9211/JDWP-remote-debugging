# Gitleaks config: default rules + project-specific allowlists.
[extend]
useDefault = true

[allowlist]
description = "Security test fixtures (fake tokens used to assert redaction)"
paths = [
  '''src/test/java/com/jdwp/client/security/SecretRedactorTest\.java''',
]
